import { Request, Response } from "express";

export function randomArticlesCtrl(req: Request, res: Response) {
  return res.json({});
}

export function organisationsListCtrl(req: Request, res: Response) {
  return res.json({});
}
